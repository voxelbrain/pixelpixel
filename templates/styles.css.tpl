body {
	padding: {{.Spacing}}px;
}

#pixels {
	width: {{.TotalWidth}}px;
}

.pixel {
	width: {{.PixelSize}}px;
	height: {{.PixelSize}}px;
	margin-right: {{.Spacing}}px;
	margin-bottom: {{.Spacing}}px;
}

