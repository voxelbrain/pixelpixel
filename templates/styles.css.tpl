#pixels {
	padding-top: {{.Spacing}}px;
	width: {{.TotalWidth}}px;
}

#pixels .pixel {
	width: {{.PixelSize}}px;
	height: {{.PixelSize}}px;
	margin-right: {{.Spacing}}px;
	margin-bottom: {{.Spacing}}px;
	border: {{.BorderSize}}px;
}

